def brasser_cartes():
    global brasse_restant

    # Décrémente le nombre de brassé restant au joueur
    brasse_restant -= 1
    #print(cartes_br)
    
    cart = en_ordre(cartes_br[:13])
    ligne2 = en_ordre(cartes_br[13:26])
    ligne3 = en_ordre(cartes_br[26:39])
    ligne4 = en_ordre(cartes_br[39:])
    
    for i in ligne2:
        cart.append(i)
    for i in ligne3:
        cart.append(i)
    for i in ligne4:
        cart.append(i)
    
    copie= cartes_br.copy()
    
    tab_enleve = []
    for i in range(len(cart)):
        
        if cart[i] == 99:
            copie.remove(i)
            tab_enleve.append(i)
            
    print("tab",tab_enleve)
    #print(cart)
    # Brasse les cartes
    brasser(cartes_br)
    print(cartes_br)
